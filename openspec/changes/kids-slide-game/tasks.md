## 1. 專案骨架

- [x] 1.1 使用 Vite 建立 TypeScript 專案骨架（無框架，純 TS + Canvas）
- [x] 1.2 建立基本目錄結構（`src/track`、`src/pieces`、`src/session`、`src/render`、`src/main.ts`）
- [x] 1.3 建立 Canvas 畫布與 `requestAnimationFrame` 遊戲主迴圈骨架（固定時間步更新）

## 2. 軌道系統（slide-track）

- [x] 2.1 定義區塊資料型別：`straight`、`curve-left`、`curve-right`、`empty`，含 `entryDirection`/`exitDirection`
- [x] 2.2 實作捲動視窗內軌道區塊的生成與移除（維持前方可視範圍內區塊數量、移除已通過區塊）
- [x] 2.3 實作 `empty` 區塊的隨機生成邏輯，含隨難度提高的出現機率與最大連續空格數限制
- [x] 2.4 實作「可解性」檢查：確保每個生成的 `empty` 區塊都存在至少一種零件型別可合法銜接前一區塊的 `exitDirection`
- [x] 2.5 撰寫軌道生成邏輯的單元測試（涵蓋連續空格上限、可解性保證）

## 3. 零件佇列與拖放（piece-placement）

- [x] 3.1 實作固定長度的零件佇列資料結構與隨機補充邏輯
- [x] 3.2 在 Canvas 上繪製佇列 UI（顯示待放置零件）
- [x] 3.3 實作 `pointerdown`/`pointermove`/`pointerup` 拖曳互動：開始拖曳、跟隨游標、放開判定
- [x] 3.4 實作放置合法性檢查（放開座標是否落在 `empty` 區塊範圍內 + 方向是否與前一區塊 `exitDirection` 相容）
- [x] 3.5 實作放置成功後更新軌道區塊型別、移除並補充佇列零件
- [x] 3.6 實作放置失敗時零件退回佇列原位的邏輯
- [x] 3.7 實作放置成功/失敗的視覺回饋（區塊高亮 / 零件閃爍紅色）

## 4. 遊戲流程與難度（game-session）

- [x] 4.1 實作遊戲狀態機：`menu` → `playing` → `gameOver`（含重新開始回到 `playing`）
- [x] 4.2 實作小朋友沿軌道自動下滑的移動邏輯
- [x] 4.3 實作依存活距離計算下滑速度的難度曲線函式（含速度上限封頂）
- [x] 4.4 實作每影格檢查小朋友所在區塊是否為 `empty` 的掉落判定，觸發後立即進入 `gameOver`
- [x] 4.5 實作本局分數計算（依存活距離即時更新）與畫面顯示
- [x] 4.6 實作 `localStorage` 最高分讀取、比較與更新
- [x] 4.7 實作 Game Over 畫面（顯示本局分數、最高分、重新開始按鈕）與重新開始時的狀態/資料重置

## 5. 渲染與整合

- [x] 5.1 繪製小朋友角色（可用簡單幾何圖形佔位）與其在軌道上的位置
- [x] 5.2 繪製軌道區塊（不同型別使用不同顏色/圖形區分，`empty` 區塊明顯標示為空)
- [x] 5.3 整合軌道捲動、小朋友移動、零件佇列 UI 於同一個渲染迴圈中
- [x] 5.4 加入基本 UI 疊層（目前分數、最高分）於畫面上方

## 6. 測試與驗證

- [x] 6.1 手動遊玩測試：驗證放置合法性判定（方向相容/不相容）皆符合預期
- [x] 6.2 手動遊玩測試：驗證掉落判定於觸碰 `empty` 區塊時立即結束遊戲
- [x] 6.3 手動遊玩測試：驗證難度曲線在長時間遊玩後速度不會超過上限、且仍可操作
- [x] 6.4 驗證 `localStorage` 最高分在重新整理頁面後仍保留正確數值

## 7. 修正：佇列可解性保證

- [x] 7.1 修正玩家回報的問題：佇列偶爾沒有任何零件方向能合法銜接目前空格，導致玩家卡死等死
- [x] 7.2 實作 `PieceQueue.ensureContains` 與 `ensurePendingSegmentSolvable`，在空格等待填補時保證佇列至少有一個合法零件，且不覆蓋玩家正在拖曳的零件
- [x] 7.3 撰寫單元測試涵蓋補入邏輯與拖曳中零件不被覆蓋的情況

## 8. 美術：滑梯造型圖片

- [x] 8.1 繪製直線/左彎/右彎三種滑梯零件的 SVG 圖片（`src/assets/slide-*.svg`），取代原本的純色色塊
- [x] 8.2 建立 `pieceImages.ts` 圖片載入器，渲染時優先畫圖片、載入完成前 fallback 回純色色塊
- [x] 8.3 空格提示改為半透明顯示「需要的滑梯造型」預覽，取代原本的箭頭圖示
- [x] 8.4 修正玩家回報的「滑梯銜接不上」問題：原本的長條滑梯圖案需要跨格邊緣對齊才會好看，改款為獨立的「兔子洞」造型（圓形洞口＋方向漩渦箭頭），每格自成一體，不再需要與相鄰格子邊緣對齊
- [x] 8.5 依玩家提供的參考圖，改為連續地下隧道風格：整個畫面背景改成泥土紋理（含隨機石頭裝飾、無限鋪磚），已鋪設的軌道改用平滑曲線（`buildSmoothPath`）畫成一條連續的隧道，取代原本逐格畫圖的「兔子洞」設計；移除不再使用的 `pieceImages.ts` 與 SVG 圖片素材；空格提示改為半透明虛線延伸隧道＋方向箭頭；佇列圖示改為土色方塊＋箭頭
